
     //Initialize the count as 0
     //listen to the click of the increment button
      //Increment the value of count each time the button is clicked
      //change the count-elem in the Html to reflect the new count
     
      function fun1(){
        console.log(42)
      }

      fun1()